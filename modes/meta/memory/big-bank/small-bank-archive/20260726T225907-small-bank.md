## 2026-07-10 23:12 (session 4a6d6f)
Removed health-agent subsystem entirely in two commits (b24db05 for the full subsystem cut, 0072874 for a companion pipeline fix), cleaning 26 files and resolving 1488 lines—work is complete and staged for push. Architecture documentation still references health_agent in product-vision.md and architect/context.md; those docs weren't touched since cleaning them is architect-mode territory and can be deferred. Working tree is clean and nothing has been pushed yet.

## 2026-07-11 10:43 (session 9eec8b)
Triaged `master_todo` by categorizing each remaining entry as resolved (with evidence for safe removal), actual issue (concrete action needed), or open question (unclear scope or still live but unresolved). Added findings inline under each line to explain the status — resolved entries document why they're settled, actual issues identify next steps, and open questions flag decisions deferred to you. The file is now structured for cleanup and actionable; next steps are to delete resolved entries, file GitHub issues from the actual-issue entries, and decide which open questions are still worth pursuing.


## 2026-07-11 12:13 (session 2d2494)
The session reframed the architect mode context by cleaning up README.md to avoid duplicating product-vision.md, removing the "open design questions" section per the todo, and de-literalizing the grove-as-plugin metaphor in product-vision.md and architect/context.md (groves are not literally in the plugins/ directory—they're generative specs relying on infrastructure, not infrastructure plugins themselves). The public website (diotima-garden.github.io) was also reworked with a new vanguard pitch, dedicated Groves and conductor sections using the player/music metaphor, and a viewport layout fix. Three sets of changes remain uncommitted in the main repo (README, product-vision, architect/context) and the website (local clone ready to push) awaiting confirmation before commit and publication.

## 2026-07-11 14:00 (session a1dcb1)
The user redesigned the public-facing website `diotima-garden.github.io` with a terminal-style aesthetic: reduced text, blinking cursor, and content formatted as command output to represent how visitors found Diotima to learn. A tree with a red apple was positioned right, and phrases loop through an obfuscated animation with inspirational suffixes ("like they teach it at Harvard," etc.), feeding terminal-output snippets below. The phrases were consolidated into `phrases.js` as tuples with prompt, output, and GitHub handle, and an easter egg was added: clicking the apple fades the terminal to 30% opacity and full-screen unfades a snake image for ~3.2s. The snake.png background transparency was fixed in the last step, so the asset now overlays cleanly without a white/gray box.

## 2026-07-11 14:44 (session f3a6cf)
Architect mode was loaded with full context: design philosophy (non-duplication, layered responsibilities), product vision (Diotima—groves as sovereign packages, model-agnostic infrastructure), and recent session work (docs reframed around grove ecosystem, health-agent subsystem removed). Working tree is clean and `master_todo` is triaged into 3 concrete items (session crawler hardcoding should externalize like mem-bank's `subscriptions.json`, Anki pipeline needs QC/review step, feedback history should inform future card generation) plus 2 open questions awaiting the user's call. Ready to move forward on any of these structural changes.

## 2026-07-12 16:24 (session 266c20)
User entered architect mode and reviewed current project state; model switched to Fable 5. One trivial uncommitted diff remains in `architect/context.md` (a path-format fix). The `master_todo` file has three concrete action items: externalize session-crawler regexes into config, add QC/review to the card pipeline, and feed accumulated feedback history into card generation, plus two open decision questions about the feedback-marker system and manual small-bank bootstrapping. Ready to work on any of these priorities.

## 2026-07-12 20:39 (session 74826f)
Builder mode is now active with key constraints around skill/pipeline scope, security (one command per block, relative paths, whitelist discipline), clean UX (no prompts/errors in pipelines), and isolated infra PRs before feature work. The session reviewed three pending infrastructure items: externalizing session-crawler regexes, adding QC to the card pipeline, and feeding feedback history into generation, plus two open design questions around feedback markers and manual bootstrapping. Some uncommitted changes exist (`architect/context.md` path fix) and an untracked file (`reditanki.txt`), so the next step is clarifying which item to tackle and handling the working-tree state.

## 2026-07-12 21:10 (session 80ea37)
Created a new `/what-a-nice-example` skill to streamline README example documentation, avoiding manual screenshot/transcript coordination; restructured the anki-mcp README with screenshots and a walkthrough showing the feedback-loop process. Posted the Reddit announcement to r/Anki with links to the repo and the documented example, currently awaiting approvals and monitoring for community questions that signal future scoping needs. Still open: the broader adoption strategy question (personal outreach to universities vs. automation, building internet presence, learning from precedents like Linus Torvalds) and waiting for r/AnkiLanguageLearning approval to cross-post.

## 2026-07-12 21:32 (session 7e1ed2)
User confirmed in architect mode that moving feedback processing skills into anki-mcp was the right decision—the coupling already points there, as the skill consumes anki-mcp's internal libraries for managed note types and feedback handling. The decision surfaced a portability issue: skill paths are hardcoded to `plugins/anki-mcp/...` instead of using `${CLAUDE_PLUGIN_ROOT}`, which breaks standalone plugin installs; this was filed as anki-mcp#3. A secondary consistency issue remains: `anki-backup-deck` sits at root despite being grove-blind, creating asymmetry with `process-user-feedback-on-deck` now in the plugin. The plugin's documentation and boundaries need updating to reflect that it's now a tool/skill provider, not just a library.

## 2026-07-12 23:38 (session ae1adb)
The session clarified that the user's project already has agentic architecture (orchestrator + isolated worker patterns) but lacks automation triggers and async digests—the gap between manual orchestration and "massive things growing overnight" is essentially a cron scheduler. The architect analysis identified that mem-bank job scheduling is the concrete first step: converting manually-triggered memory consolidation into continuous background work using existing infrastructure, avoiding the complexity of rebuilding for hypothetical future workflows. The vision and todo files are intact; the next move is to prototype scheduled mem-bank jobs via the existing worker pattern to prove the model works, then expand to other judgment-free automation opportunities. Work can resume by scheduling the mem-bank job runner (smallest honest version of async agents) and observing whether that closes the gap between what the user perceives others doing and what's actually needed here.

## 2026-07-13 11:23 (session acc038)
Implemented a PreToolUse hook under `system/scm-integration/` that enforces at least one label on every GitHub issue created via `gh issue create`. The hook discovers existing repo labels at block time and proposes them in stderr, eliminating the need for the model to discover them itself — a single retry reads the menu and re-runs. The gate degrades gracefully when label discovery fails (offline, bad repo, etc.) by falling back to instructing the model to run discovery manually. Implementation is committed as `9ff6f65` and working against the real repo; this resolved the earlier discussion about semantic enforcement through hooks rather than prose rules in `.claude/rules/`.

## 2026-07-13 15:22 (session ae1adb)
User completed an agentic-workflows architecture assessment: concluded mem-bank is their project's core differentiator and no mature open-source solution fits their constraints (plain markdown, human-auditable, local-first, memory portable inside groves), so ownership wins over adoption. A video critique of mem-bank plus architectural analysis surfaced four concrete improvements: tool-failure capture in session summaries, decay metadata for forgetting, graduation-lifecycle tests, and unattended scheduling with morning digest. The assessment also carried three prior items from `master_todo` that were ready to action. Eight GitHub issues were filed across mem-bank (#5–#8), carried-over infrastructure (#9–#11), and observability (#12); opencode-claude-memory was found and saved to persistent memory as a migration reference rather than actioned as a task. The three `master_todo` entries behind issues #9–#11 remain in the original file and could be cleaned up now that they've been migrated to the issue backlog.

## 2026-07-13 16:16 (session 7491c5)
The session moved the world-adoption infrastructure forward by designing and implementing a disclosure system for AI-generated content: the user architected a governance contract, a `prepare-human-facing-content` skill that prepends a hardcoded paragraph acknowledging AI generation and the user's time constraints, and an audit ledger to track outbound posts. During implementation, a broader architectural insight emerged around mem-bank's design — filed as diotima#13 — distinguishing between deterministic extractors (for transcript content) and boundary-captured events (via PostToolUse hooks on posting tools), with the ledger staying human-confirmed for now. Six files landed in the commit (`dc94af1`), leaving the infrastructure ready for actual posting-tool integration; the issue captures the generalization pattern for future mem-bank work.

## 2026-07-13 17:54 (session 44f4b2)
The user pivoted from building a project-level Reddit MCP to an architect decision: keep Reddit integration as private user-space tooling rather than version-controlled infrastructure. After verifying that nothing was publicly committed (the GitHub repo exists but is empty, submodule uncommitted, cookie work disk-only), the risk is fully reversible and the reputation exposure is zero. The plan is to move the reddit-mcp directory to user space (e.g., `~/.claude/mcp-servers/reddit-mcp`), register it at user scope in `~/.claude.json`. All is now done

## 2026-07-13 18:46 (session 020e24)
The session focused on world adoption outreach: the user posted an AnkiAi law workflow reply to Reddit (r/AnkiAi thread 1uct658) using the `/prepare-human-facing-content` skill, and requested two infrastructure improvements afterward. The skill was enhanced to surface the posting destination (link/address/thread) so the user doesn't have to hunt for it, and the ledger now records automatically on confirmation rather than prompting the user—the latter required adding an `Edit(modes/world-adoption/outbound/ledger.md)` permission rule, which is very permissive (auto-approves any edit to that file, not just appends). One open concern: the user may want to revert that broad permission rule if they'd rather keep a checkpoint on ledger rewrites; if they confirm that preference, the rule should be dropped.

## 2026-07-14 17:32 (session da3a78)
User clarified the boundary between project knowledge and personal memory: knowledge about world-adoption practices (like transparency about self-editing posts) should live in the project's `modes/world-adoption/outbound-contract.md` and related files, not in the agent's memory system. The assistant removed three wrongly-written memory files and moved the relevant clarification into the outbound-contract itself, establishing that project-specific context belongs in the codebase, not memory. This reinforces the pattern that infrastructure context (skill design, pipeline rules, disclosure practices) should be documented in CLAUDE.md and mode-specific files where future work will naturally reference them.

## 2026-07-14 19:13 (session a5d04d)
Converted submodule URLs from SSH to HTTPS in both top-level and nested `.gitmodules` files to unblock external users, while adding a `git config` rewrite rule that transparently maps HTTPS clones back to SSH for your personal pushes. Discovered and fixed a hook bug in `.claude/settings.json` where relative paths could deadlock if cwd shifted — rewrote all hooks to use `$CLAUDE_PROJECT_DIR` for robustness. Committed the three files as `0a4aaea` (top-level); the nested anki-mcp commit `280e543` exists only locally and must be pushed first before pushing master.

## 2026-07-16 01:23 (session ec59b4)
User entered builder context and the assistant prepared to review current builder-mode work items in `master_todo`, but the request was interrupted before any work commenced. No changes were made; the working tree remains clean at the last commit.

## 2026-07-16 01:47 (session 8b23a5)
A new `/onboard` skill was created to guide newcomers through the project setup process, replacing a manual paste-the-prompt approach with an automated setup audit and diagnostics flow. The skill shipped in commit `fdca5f4` with whitelist entries and documentation, and is now live on master — it will receive its first real-world test when Salitur attempts their clone and runs the command you've already told them about. Open threads remain: monitoring that first test for friction reports, the r/AnkiLanguageLearning cross-post awaiting approval, and the deferred university-outreach decision.

## 2026-07-17 18:50 (session f1eedd)
In architect mode, the user reviewed and corrected the raw grove-inheritance dictation file, which serves as a decision record source-of-truth. The assistant identified 24 ASR mishearings (predominantly "grove/groves" misheard as "growth/grows/Bros," plus substitutions like "four King"→"forking," "bass class"→"base class," and "automatic rag"→"automatic RAG") and applied all corrections in place, preserving the file's raw stream-of-consciousness texture while restoring fidelity to the actual spoken words. The work is complete; the decision record now has improved accuracy for future architectural reference.

## 2026-07-17 18:53 (session 19284e)
The session resolved the grove-inheritance architectural decision by clarifying priorities: future-proof architecture trumps preserving existing component mechanics, memory banks use author's git choice (commit vs. gitignore) rather than enforced format, and the unnecessary `id` field was removed in favor of git content-addressing and parent repo URLs. The main open question is the **connection protocol** for standalone groves — whether new repos should connect to the main diotima orchestrator via environment variables, directory infrastructure, MCP, or another mechanism still to discover — plus the interaction model (user initiates from diotima or grove itself). The architectural record and user's original raw dictation are on disk but untracked; `modes/meta/architect/context.md` still has half-finished edits (empty "Core Pattern" heading, truncated bullets) that will need cleanup next session.

## 2026-07-18 11:19 (session 40063c)
**CVUT outreach Phase 2 complete: 573 subjects scored and 311-subject shortlist created.** The user kicked off world-adoption mode wanting deterministic subject harvesting and scoring, and the session executed a full parallel-batch relevance evaluation using Haiku, producing `phase2/relevant_subjects.csv` and `phase2/all_scored.csv` with all subjects in the shortlist having guarantor emails and profile URLs for Phase 3 contact work. Haiku's scoring was generous (54% made the shortlist), so Phase 3 should prioritize high-signal subjects like team/software projects, applied ML, architecture, HCI, and knowledge-management courses over foundational programming. **Two follow-ups pending:** commit the Phase-1 crawler, Phase-2 skill, and all data to disk; or log the thread to world-adoption mem-bank so the next Phase-3 session has full context and doesn't need to re-derive the scoring decisions.

## 2026-07-18 11:48 (session 4d874e)
Session completed the DAFNE architectural move: built a full dafne_simulation/ sandbox with node inheritance (grove, language, spanish) using symlinked parents, created DAFNE.md manifests for each node, and documented architectural decisions including the node/grove split, removal of `parents:[]` from manifests (delegating to git submodules), and reservation of `.private/` for node-private scripts. Three precise open items remain: D1's freeze vocabulary and union-up-the-tree question, D4's pin-vs-range decision (leaning toward pins via the parents drop), and the production groves/ repo migration that the sandbox validated. Everything is committed and ready for the next session to resolve those decision points and extend from simulation to actual repositories.

## 2026-07-19 12:58 (session f63b83)
The architect context was refactored into a thin persona pattern: `context.md` (~35 lines) holds the role definition and operating principles with lazy-loaded pointers to design references, while `layers.md` and `structural-lessons.md` are loaded only when designing. This structure supports future migration to an opencode agent definition. The change was committed as `fd84ecc`. Three files remain uncommitted in the working tree (`major_architectural_decision_to_be_made.md`, `interaction_north_star.md`, `cvut_mail.txt`) — ready to push when you decide.

## 2026-07-19 13:02 (session 2fafc3)
User gained orientation on the mem-bank system (capture via `small-bank.py` hooks at SessionEnd, job queueing, graduation to big-bank, subscription-based pattern matching) and its core design tenets: mechanical triggers via hooks, LLM for judgment content, append-only state, and fail-silently recursion guards. A fork analyzed `modes/meta/architect/context.md` and identified it as under-utilizing the project's hard-won lessons while carrying generic filler—proposing additions on silent-failure observability (require logging contracts at lifecycle boundaries), idempotency of producers and event re-firing, and ~7 other structural lessons backed by real incidents. The rewrite is blocked pending resolution of modified and untracked files in the architect directory (`major_architectural_decision_to_be_made.md`, `interaction_north_star.md`).

## 2026-07-21 20:34 (session 3219b8)
Session clarified the user-experience topology for Diotima by sketching three interaction modes (direct grove opening, git-model launcher, managed library) that operate over the same underlying infrastructure, leading to a new `interaction_north_star.md` documenting the vision. Phase 0 of the DAFNE execution plan concluded with golden snapshots committed and verified against real production content, confirming that the D0 spike (SessionStart hook availability) unblocks the runtime; D4 was settled in the decision record toward pins plus assisted-update UX rather than version ranges, deferring the range field as future-additive. Phase 1 now awaits: creating a standalone DAFNE engine repository under `plugins/dafne`, absorbing context-compiler, and implementing manifest reading with requires-union — pending confirmation whether to proceed or if the user will set up the external repo first.

## 2026-07-21 20:34 (session 327fae)
The session was briefly opened, cleared, and the user initiated a request to enter architect mode but interrupted before any work commenced. No substantive changes, explorations, or decisions were made during this session. Nothing to append to project memory.

## 2026-07-21 20:44 (session 436d23)
Architect mode was entered and context loaded, revealing DAFNE Phase 0 complete (golden snapshots verified, SessionStart-hook unblocks runtime) with D4 settled on version pins and interaction model sketched across three UX topologies. DAFNE Phase 1 is queued to create a standalone engine repo under `plugins/dafne`, absorb `context-compiler`, and implement manifest reading with requires-union; the connection protocol for standalone groves—how forked grove repos reconnect to the diotima orchestrator—remains the main unresolved architectural point. Three uncommitted world-adoption items sit in the working tree unrelated to DAFNE. Next step: choose whether to kick off Phase 1, tackle the connection protocol design, or pursue something else.

## 2026-07-23 19:20 (session 6853c9)
User completed an end-to-end test of the Spanish deck feedback-processing pipeline, running backup, user-feedback extraction and LLM-driven edits, and confirmation—all succeeded (exit 0). Changes were committed and pushed to both the diotima parent repo and anki-mcp submodule; a known issue (anki-mcp#4) tracking missing `TimeoutExpired` handling in the retry loop was filed but not fixed. Two untracked files remain (CONTRIBUTING.md, modes/meta/architect/packaging-strategy.md) that weren't part of this session's work. The workflow validates that the feedback-application infrastructure runs cleanly end-to-end.

## 2026-07-23 21:51 (session c9a274)
The `youtube-extract` skill was modified to append extracted phrases to `youtube-phrases.txt` instead of overwriting it, enabling safe accumulation of multiple video extractions in a single run. The change was made to `extract.py` and committed as `2dfada7`. The skill continues to ignore the output file (`.gitignore` already in place), keeping only the source code under version control. The session was complete; no follow-up work is pending.

## 2026-07-25 23:23 (session 7429eb)
Created GitHub issue #16 on the diotima project to enforce stricter `bank_capture_prompt` handling in `registry.py`: the feature should make `this-bank-prompt.md` required by default (with opt-out via `subscriptions.json` flag), and currently returns `None` silently when missing or empty, allowing unfiltered capture. The issue scope includes unit tests for present/missing/opted-out/empty/cwd cases, plus LLM-in-the-loop verification that the filter actually rejects off-topic sessions, with a "cheeky" adversarial test that tries to trick the model into ignoring the filter while discussing Anki edits. Implementation of both the strictness feature and test suite remains open.

## 2026-07-25 23:59 (session d2f497)
A plugin dependency resolution gap was identified and documented as GitHub issue #17 (labeled `architect`). The repo had already decided the contract for `requires:` declarations, the delivery mechanism (uvx), and the ambient runtime topology, but lacked the concrete binding that turns a grove's `requires: anki` into an installed, activated plugin on the user's machine. The issue was framed around that specific seam using the repo's "discriminating test" method, fixed points were listed as do-not-re-litigate, and the four remaining sub-decisions were surfaced as distinct open questions. Issue #17 was linked as a sub-issue of the broader #4, establishing it as a focused architectural slice of the larger grove redesign effort.

## 2026-07-26 00:03 (session 485257)
Filed diotima#18 proposing structured trigger declarations for memory banks — the core critique is that stage-1 triggering matches regex against raw transcript text, creating false positives (paths printed in output) and false negatives (edits to sibling files). The proposed fix leverages existing `session_crawler` structured parsing to trigger on actual path operations and tool types, with validated JSON schema declarations replacing bare regexes. The issue is positioned as the "trigger/routing side" relative to #13 (output filtering) and #16 (bank-prompt filtering), keeping the permissive regex as a catch-all. A process note flagged that this mem-bank/builder research arguably should have entered builder or architect mode first — that's a pattern to remember for future architecture-framing work.

## 2026-07-26 00:06 (session 9ebdd2)
Architect mode designed the `social-dynamics` grove structure around a `drills/dating-frame/` nesting unit, moving the root `soc_dynamic.md` simulator to `groves/social-dynamics/drills/dating-frame/artifacts/soc-dynamic-gemini-first-attempt.md` with drill-specific rubric (Subtext/Leverage/Thermodynamics) and grove-level `principles.md` for cross-cutting stance — all spelled out in `social-dynamics-grove-plan.md` at repo root with explicit decision docs and deferred seams (Anki use, tool selection, research evidence collection). No files were created or moved; the plan is ready as a turnkey builder-session recipe. Still open: tool choice for the memory bank (text-only vs. Anki integration), external tool integration, and eventual research proof of simulator efficacy.

## 2026-07-26 00:09 (session cc6b0e)
An architectural question was raised about where the architect and builder modes should belong in the repo structure: as separate groves, as agent concepts in the Diotima runtime orchestrator, or integrated into an existing grove. The analysis identified a genuine collision in repo definitions—DAFNE structural rules make them look grove-shaped (they have registered mem-banks), but product-vision.md positions agent definitions in the runtime, not in groves. The issue frames this as a definition-governance question rather than picking one answer, and notes that architect and builder currently share one `meta` bank, which complicates the "separate groves" path and likely applies to `world-adoption` mode as well. A GitHub issue (#19) was opened to track this decision point; the next step is alignment on whether the structural or vision definition governs placement.

## 2026-07-26 00:13 (session 8633e0)
The session started executing the social-dynamics-grove-plan.md in builder mode; the first step added the grove to CLAUDE.md's Navigation table pointing to `groves/social-dynamics/context.md`, matching the pattern of other groves and noting the text-only (no-Anki) structure. The remaining steps of the plan — creating directories, moving the simulator artifact, writing context/principles/rubric files, setting up the memory bank, and registering it in `subscriptions.json` — have not yet been executed. Resume by running steps 3.1–3.10 sequentially from the plan document.

## 2026-07-26 10:07 (session 202fb2)
The session created three GitHub issues across the anki-mcp and diotima repos addressing infrastructure concerns: card feedback-driven state changes (unsuspend/unburry), extending card creation logs to capture source text via the existing `log` field mechanism, and completing Gemini extraction by moving `.claude/gemini` into a dedicated `utils/llm_invoke` hierarchy (noting that `youtube-extract` specifically needs Gemini's video-URL capability rather than a generic multi-provider abstraction). All three issues flagged specific design decisions (where seeding happens, granularity, field-size constraints; target location vs. precedent; eventual provider split) without prescribing solutions, and adopted existing patterns (the `log` field is already managed by `_update_note_fields`, `utils` is already a separate repo, mem-bank extraction was already tracked) to ground the proposals. Implementation decisions remain open pending issue discussion.

## 2026-07-26 11:24 (session 8ae3c0)
User created two linked GitHub issues addressing mem-bank infrastructure design: #24 proposes centralizing the duplicated per-bank `context.md` files into a single `.claude/mem-bank/` template instead of hand-copying them across `modes/meta/memory/`, `groves/social-dynamics/memory/`, etc., while #25 frames a design decision between the current semantic read pattern vs. a hook-driven `session_crawler` injection for small-bank context. Issue #24 is likely blocked on #21 (mem-bank extraction), and #25 connects to existing trigger-quality work (#18). Both issues are exploratory/design-stage; implementation has not started.

## 2026-07-26 17:17 (session 6b6265)
The user designed a comprehensive packaging strategy for Diotima (the project's orchestrator) in architect mode, evaluating Docker-based, local installation, and hybrid approaches to make the system with its Anki integration trivially runnable for users. A packaging strategy analysis document was created capturing multiple approaches, their user-setup workflows, and UX trade-offs—explicitly excluding Claude plugins and noting that a CI container could coexist with normal Anki setups. The strategy document was committed with a corresponding GitHub issue emphasizing the need to design convenient installation mechanisms for Diotima's infrastructure (Anki, Obsidian, and future tools) to eventually support simple GUI versions of standalone DAFNE groves. A `CONTRIBUTING.md` file was created and committed as `1803d9e` on master, with `groves/social-dynamics/context.md` remaining uncommitted.

## 2026-07-26 17:25 (session d86a84)
The session explored plugin and tool integrations for the DAFNE architecture, with social-dynamics grove serving as a key validation case: it proved the tool layer (Anki, runtime integrations) is optional and additive, not foundational—a grove works with just context files and an LLM. Two investigation issues were created for potential new `requires:` entries: audio-tts (#29, for pronunciation generation) and intake (#30, for Zotero/Readwise-style source tooling, which pins the A-vs-B category boundary question). Both issues reference #17 (the prerequisite plugin-binding mechanism) and follow Phase 5's zero-instance discipline by framing as investigation, not speculative builds. The Anki leak-log from social-dynamics was not given its own issue since it's already tracked in the grove's improvement-plan.md as an existing-capability reuse.

## 2026-07-26 18:40 (session 7f0792)
User investigated GitHub issue 21 regarding extracting the memory bank system into a separate repository. After exploring integration points and engine internals, the user chose to create a standalone repo (`diotima-garden/mneme`) rather than moving the session crawler to utils. The extracted mem-bank will mount at `.claude/mem-bank` in the main repo (following the existing pattern for python-utils), while the session crawler's interface will remain stable to accommodate future detection of execution context (opencode, Claude, etc.). An implementation plan has been drafted and is ready for execution on a new branch.

## 2026-07-26 18:45 (session b6f75e)
The user discovered that plans created in plan-mode don't persist as repo files after `/clear` — they're buried in raw session transcripts — and established a pattern to fix this: write approved plans to durable files under `modes/meta/architect/` (following the existing examples like `dafne_plan.md`). An attempt to execute a recovered plan in builder mode was underway but interrupted before the full plan was retrieved or written down. Resume by recovering the plan from the prior session's transcript, persisting it to a file, then proceeding with builder mode execution once the four reference docs are read.

## 2026-07-26 18:46 (session c02423)
During architect review, you decided to migrate Anki-related skills into the anki-mcp submodule and made four specific structural decisions: remove Spanish-specific coupling, move idiom-batch auto-suspend to the languages grove with a tracking issue, make anki-add-cards pipeline-only (invisible to users), and resolve the skill/pipeline name collision. The assistant had begun gathering reference lines and verifying how the worker should receive deck names to become grove-blind, but the work is incomplete — the exact parameter passing for `add_notes` and the full scope of reference updates still need to be mapped out.

## 2026-07-26 18:48 (session 7f0792)
Session focused on structuring GitHub issue 21: extracting the mem-bank engine into its own repository. After exploring integration points and internals, decided to create a standalone repo (`diotima-garden/mneme`) for the mem-bank engine while keeping the mount path in the main repo as `.claude/mem-bank`. The session crawler component will remain structured with awareness of different execution contexts (claude, opencode, etc.) while maintaining a stable interface. A fresh worktree at `../mneme-extraction` on branch `mneme-extraction` now contains the implementation plan as `PLAN.md`, ready for continuation in a separate session with clean context.

## 2026-07-26 22:45 (session c02423)
Executed relocation of anki skills into anki-mcp submodule, decoupling them from Diotima-specific knowledge (groves, managed-models) to support future DAFNE portability, moving Spanish-specific logic to separate issues, and making anki-add-cards pipeline-only rather than user-visible. After committing, discovered and fixed incomplete staging of the pipeline file's content edits via git amendment. The relocation is complete and ready for testing; submodule and parent repos are synchronized and unpushed.

