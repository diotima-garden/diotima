## 2026-05-07 17:07 (session 70a8e7)
Session transformed `session_crawler` from a functional shim layer into a class-based interface by moving it to its own directory with test infrastructure. The work involved creating example JSONL transcripts as test fixtures, retiring 27 passing unit tests, and migrating existing callers (`small-bank.py`, `detect-health-issues.py`) to use `SessionTranscript` directly. Backward-compatibility shims were deleted once test parity was verified, leaving only the core `SessionTranscript` class and `extract_text` export. The new structure supports the planned enhancement to make session_crawler a queryable interface for session metadata, with all 27 tests passing and no remaining dependencies on legacy functions.

## 2026-05-10 20:58 (session e10c8b)
Implemented mode-gating for health findings surface: `surface-session-health.py` now reads stdin, detects the current session mode via `SessionTranscript`, and only processes/surfaces findings when in builder mode (exits silently for user and architect sessions). Updated `README.md` to document the mode-gate mechanism and removed stale references to `health-state.json` and throttle tuning. Tests pass (34/34). The change solves the user's core concern: health findings now won't interrupt during exploratory or architectural sessions, only during builder work where they're actionable.

