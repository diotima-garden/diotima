## 2026-05-06 22:37 (session b30804)
Merged master into lit_search and resolved `.gitignore` conflicts by keeping both branches' additions (cache/ and .claude/meta/memory/small-bank.md). Staged and committed the merge; the repository is now clean with no unmerged paths. Two untracked files remain (.claude/hooks/hooks.log and ereader-transfer.md) but don't block work. Ready to resume development.

## 2026-05-06 23:13 (session 9eedac)
User initiated builder mode and the assistant confirmed activation, verifying the current branch state (`lit_search` merged from master) and identifying two untracked files that won't interfere with work. The session is waiting for the user to specify what they want to build or modify.

## 2026-05-07 08:46 (session abeb0e)
Reading session integrated memory bank mechanics into the Spanish deck's reading log subsystem, replacing the flat `reading-log.md` with a small-bank directory structure and migrating existing content. The hook mechanism was refactored to support per-bank custom prompts, replacing the two-file handoff (`last-prompt.txt` + `last-targets.txt`) with a single `last-jobs.json` that enables one Claude call per bank instead of one shared call. File naming was clarified — `capture-prompt.md` → `this-bank-prompt.md` to reflect its role as the bank's custom prompt template. Work is complete with files staged and ready for commit.

