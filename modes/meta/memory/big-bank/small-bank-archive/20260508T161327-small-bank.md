## 2026-05-07 16:17 (session f3e79f)
Refactored logging across the anki-mcp launcher and server scripts to use a shared `make_logger` utility from `.claude/utils/log.py`. Removed duplicate logging code from `launcher.py` and added logging instrumentation to `server.py` (startup events, note counts in `add_notes`, and sync operations), with both scripts now writing to a unified `anki-mcp.log` file using consistent `[launcher]` and `[server]` tags for easy debugging.

## 2026-05-07 17:01 (session ee7f54)
Completed migration of Anki operations from bash scripts to MCP tools: added eight new tools (find_cards, find_flagged_cards, cards_info, notes_info, delete_notes, update_note_fields, set_card_flag, export_deck) with intentional architectural abstractions like flag word-to-number conversion hidden in the MCP layer. Updated three skills (anki-backup-deck, anki-process-purple-delete, anki-process-red-edit) to call named MCP tools instead of bash, and removed the anki.py bash whitelist in settings.json. The broader architectural direction is clear: build rich atomic building blocks in the MCP server for high-level operations and future statistical reasoning, rather than mirroring AnkiConnect. Next step is deciding whether to retire the skill-layer wrappers now that MCP tools are the primary interface, and verify MCP tool invocations don't trigger permission prompts.

## 2026-05-08 10:31 (session 508628)
Expanded the Anki MCP to comprehensive AnkiConnect coverage (51 tools total: 40 newly added across tags, decks, notes, scheduling, models, media, and stats). Established a permission model where read operations are silent and destructive operations (delete, suspend, etc.) prompt the user; added `update_note_fields` to the whitelist for the red-edit skill specifically. Decided to defer module introspection for now, instead taking a phased approach to gradually remove instruction detail from deck defaults as the MCP becomes smarter. Next step is verification that the whitelist is working correctly and then shifting focus to simplifying user-level deck files.

## 2026-05-08 12:45 (session 2f8223)
The user refactored the mem-bank subsystem to fix data loss from `last-jobs.json` being overwritten, renaming it to `small-jobs.json` and converting it to an append-only log where each job tracks metadata (`session_id`, `created_at`, `processed` status). The worker was extracted from the hook into a separate `small-job-worker.py` file to improve modularity, with recursion protection kept in the hook and a new `/mem-bank-run-job` skill added to manually invoke the worker against specific job sessions. The implementation is now complete with five coordinated changes across files and settings, including a bug fix where the recursion guard was initially placed incorrectly in the worker rather than the hook.

## 2026-05-08 12:59 (session 2337fc)
The session implemented MCP logging to track tool usage statistics by wrapping `@mcp.tool()` registrations in `core.py` with a thin logging wrapper that fires once per invocation using a `[call]` tag, producing timestamped log entries like `2026-05-08T13:42:11.123456 [call] vocabulary_snapshot`. The implementation avoids the inflated counts problem of logging at the `_call` level by capturing invocation at the MCP boundary instead, and requires no changes to any of the nine tool modules. To activate the logging, Claude Code needs to be restarted to reload the MCP server, after which `grep '[call]' .claude/anki-mcp/anki-mcp.log | awk '{print $3}' | sort | uniq -c | sort -rn` will provide clean usage statistics.

## 2026-05-08 13:58 (session ffdd48)
The user entered builder mode to simplify the "process red cards" skill by removing technical AnkiConnect details now that the anki-mcp server uses MCP directly. After discussion about field types, field scanning strategies, and MCP metadata capabilities, the skill was rewritten from 63 to 61 lines using a single `get_flagged_notes` MCP call and generic field scanning for square brackets instead of hardcoded field names, eliminating model-specific branching. The new implementation lets MCP handle all the technical details while the skill focuses purely on application logic. The "purple-delete" skill is identified as the next candidate for similar simplification using the same `get_flagged_notes` pattern.

## 2026-05-08 14:08 (session e0fa2c)
Red-edit skill was recently simplified to 61 lines using generic field scanning via `get_flagged_notes`, reducing boilerplate. The same MCP call pattern is present in the purple-delete skill, making it the identified next candidate for similar refactoring. Builder mode is active and ready to proceed with the next simplification task.

## 2026-05-08 14:47 (session 38c33d)
The session shifted the architecture toward model-agnostic card generation: instead of hardcoding Anki models in deck defaults or skills, the system now queries the live model registry, auto-selects or creates the appropriate model based on card content, and hides the technical model-management details behind the MCP layer. A new `create_model()` tool was added to the MCP (with cloze handling hidden inside), the add-cards skill was rewritten to fetch available models and gate creation behind user confirmation, and hardcoded model references were removed from deck configuration. The approach is ready for testing after restarting Claude Code so the MCP server picks up the new tool—the next `/pipe:anki-add-cards spanish` run will exercise the full flow, though English and Instruments compiled.md files may need similar updates.

## 2026-05-08 15:12 (session c92eae)
New MCP server tools for model customization are coded and ready (`update_model_templates`, `update_model_styling`, and `store_media_file` with URL support) but require a Claude Code restart to activate. The `anki.py` script has been deleted as it's no longer needed. Pending after restart are four API calls to customize the huba-buba model (template HTML, CSS styling, media file), then add a demo card to the Instruments deck to showcase the server's capabilities. User asked to load builder context first before proceeding with the customization work.

## 2026-05-08 15:46 (session c92eae)
Session explored extending the MCP server's template capabilities by building a local reference system for Anki model HTML/CSS syntax. Instead of fetching external docs on each use, the team implemented `anki://template-reference` as a FastMCP resource — a curated cheat sheet covering syntax, CSS conventions, multi-card patterns, and project-specific styling rules — registered in `tools/resources.py` and referenced by the model-update tools. The user confirmed that three-sided cards and advanced layouts are possible through template design, and endorsed using MCP's resource protocol as a standard, efficient way to attach reference material to tool calls without network dependency or token waste. With the resource now live after restart, the next step is verifying it appears in the catalog and measuring whether it reduces token cost and improves card-design workflows.

## 2026-05-08 16:03 (session c92eae)
The session explored Anki note-type capabilities by tweaking the huba-buba model to demonstrate multi-field HTML/CSS styling on card backs, surfacing the need for template/styling reference material. Rather than fetching external docs on each call, the team implemented a local MCP resource—`anki://template-reference`—via `tools/resources.py` to register a curated cheat sheet that distills Anki syntax, CSS patterns, and project conventions; both `update_model_styling` and `update_model_templates` now direct Claude to read the resource before use. After a Claude Code restart, the resource should be discoverable via `ListMcpResourcesTool` and callable via `ReadMcpResourceTool`, eliminating token waste and external dependencies while keeping the solution maintainable and extensible.

## 2026-05-08 16:04 (session 81bcc0)
Established a `tools/ref/` directory structure for resource documentation to keep the MCP directory clean and scalable. Moved `template-reference.md` there, created `add-notes.md` with detailed API documentation (schema, cloze syntax, field conventions), and registered both resources via `resources.py` — requires restarting Claude Code to reload the MCP server and verify they appear in the catalog. Clarified that `add-notes.md` (API mechanics layer) and the existing `inheritable-language-defaults.md` (application logic and deck design decisions) serve different purposes and are both needed; the reference documentation for cloze syntax doesn't make the defaults obsolete since they operate at different levels. The pattern is now set to add documentation per function in `tools/ref/` as more complex API surfaces need resourceifying.

