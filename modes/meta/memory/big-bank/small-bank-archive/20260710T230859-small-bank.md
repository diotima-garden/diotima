## 2026-07-10 14:35 (session 82431f)
Two documentation commits were added to master: `b858528` illustrated the mem-bank graduation flow with three screenshots added to the README and assets directory, and `7dfd84e` updated the mem-bank-big-bank command to prompt users to commit after the graduation summary. The user initially requested amending the second change into the first, then corrected to use a standalone commit instead. One untracked file remains in the working tree (`rioplatense-anki.compiled.md.feedback.jsonl`), unrelated to this session's changes.

## 2026-07-10 14:56 (session 2337f1)
User identified ambiguous documentation in the feedback processing pipeline's `SKILL.md:45` — the command description didn't clearly distinguish between "ids to apply" and "ids to skip." The assistant resolved this by refactoring `apply.py` to accept an explicit `--skip note_id ...` flag (via argparse) instead of relying on positional arguments, updated the skill description to reflect this, and removed a stale cross-reference in `confirm.py` that was coupling the two modules' interfaces. The change preserves the ergonomic happy path (calling `apply.py` with no args applies everything) and no whitelist updates were needed. The work is complete and verified.

## 2026-07-10 15:17 (session 565fc9)
Clarified the `extract.py` design: the two output shapes (minimal `edit_input.json` vs full `feedback.jsonl`) serve incompatible lifecycles (transient overwrite vs append-only audit), justifying the difference; this also feeds refine-context per `04-refine-context.md`. Moved jsonl path concatenation from skill-level string-building into code via a shared `feedback_log_path()` helper, eliminating the skill spec's responsibility for the naming convention and ensuring edit.py and extract.py stay consistent. Trimmed verbose comments across the feedback infrastructure; recorded comment-verbosity as standing guidance. All changes verified to preserve existing behavior and whitelist compatibility.

## 2026-07-10 15:38 (session 0b100a)
Tested the end-to-end feedback pipeline (extract, edit, confirm, apply) with the reset-test-deck fixture, identifying a readability gap: confirm.py was showing only changed fields, making it hard to understand the full context of each card. Improved confirm.py to display all fields plus the feedback instruction plus the before/after changes, providing clearer visibility into proposed edits. Committed the pipeline refinements to the anki-mcp submodule (3 commits including the `--skip` flag, `feedback_log_path()` helper, and the confirm output improvements) and bumped the submodule pointer in master to track them.

## 2026-07-10 16:37 (session 9f015c)
The user identified `plugins/anki-mcp/tools/aggregate.py` as a candidate for refactoring, questioning whether its utility functions are still actively used across the codebase. The investigation was captured in GitHub issue anki-mcp#1 and saved as a project memory (`aggregate-py-refactor-candidate.md`) to resurface later when time permits. No code changes were made; this is an open infrastructure design initiative awaiting deeper analysis on consolidation and removal opportunities.

## 2026-07-10 17:18 (session 853df3)
Found a sync gap in anki-mcp's `bootstrap.py`: note-type changes don't automatically trigger a sync to Anki, which is safe only because pipelines bookend with sync — but future ad-hoc callers could miss this. Instead of auto-implementing a fix, added a clarifying comment ("Deliberately does not call sync") and filed **anki-mcp#2** to track this infrastructurally, documenting the current implicit mitigation and options to contemplate (lint pipeline specs or surface a "pending sync" state), while ruling out auto-sync due to GUI dialog risk. The comment edit wasn't committed yet because `bootstrap.py` has other uncommitted changes (`enrich_model` integration, docstring rewrite, untracked `styling_infrastructure/` directory) that should be addressed first.

## 2026-07-10 17:31 (session efe222)
User completed infrastructure work to eliminate manual Anki flag-setting by creating an automated visual feedback system: a new `styling_infrastructure` directory under `managed_note_types` now enriches managed models during bootstrap with CSS rules that inject an inline-SVG red apple marker whenever the `user_feedback` field is populated. The implementation replaced an emoji approach (which rendered as tofu) with proper SVG and added explicit `z-index: 2147483647` layering to ensure the marker stays on top; both Production and Cloze models were tested live and confirmed working, with commits pushed to both the anki-mcp submodule and master. The Cloze model will pick up the marker automatically at next server startup via `bootstrap.run()`, and user is testing that flow.

## 2026-07-10 17:40 (session af88a3)
User entered builder mode to work on infrastructure. The session loaded context about ongoing work: bootstrap.py has uncommitted changes (`enrich_model` integration, docstring rewrite) and an untracked `styling_infrastructure/` directory, while anki-mcp#1/#2 remain open infrastructure items. The user didn't specify what to prioritize next, so the next step is to clarify which piece of work to tackle first.

## 2026-07-10 18:14 (session e69def)
Verified that the anki-mcp styling and feedback unit tests (`test_injection.py`, `test_enrich_integration.py`) are intentionally dependency-free, using plain `assert` statements and hand-rolled test loops rather than pytest — so the venv setup in the README is already complete and needs no pytest addition. The venv contains only the core dependencies (`mcp`, `httpx`, `jsonschema`) as documented, confirming that pytest was never intended to be part of the anki-mcp test infrastructure. No action items remain from this session.

## 2026-07-10 18:16 (session 23fe0b)
The session refactored the context-compiler infrastructure to emit self-explanatory semantic tokens (e.g., `FRESH`, `STALE`, `BROKEN_INCLUDE`) instead of numeric exit codes, eliminating friction around permission prompts and path guessing. All venv-prefixed commands now match explicit whitelist entries in `.claude/settings.json`, and the skill files (`compile-context/SKILL.md`, `reverse-propagate/SKILL.md`) were simplified to just run the script and follow its output message rather than re-explaining exit-code logic. The test suite was updated to assert on tokens and exit-0/nonzero behavior, and all 19 tests pass. The original task—processing flags in the Spanish deck via the process-flags pipeline—was interrupted and remains incomplete.

## 2026-07-10 19:12 (session bb0034)
The session focused on processing user feedback in the Spanish Anki deck using the `/pipe:anki-process-flags` pipeline, which executed a multi-step workflow including deck backup, feedback processing, and cleanup of flagged cards—the user selectively skipped two cards (5 and 17) with specific feedback about simplified explanations and front-side alignment. After processing, the user entered builder mode to enhance the anki-mcp documentation by adding a series of screenshots from the editing workflow to the README and creating an assets directory, including before/after card states to illustrate the feedback transformation journey. The session concluded with the user committing changes across both the main repository and the anki-mcp submodule separately, then pushing both to their remotes. No open items or follow-up work remains—the full pipeline executed, documentation was enhanced with visual examples, and all changes were committed and pushed.

## 2026-07-10 19:12 (session dfd184)
The session was minimal and interrupted before substantive work began. The user cleared context, indicated interest in entering builder mode, but the request was interrupted before proceeding. No changes were made to the codebase and no task was left in progress.

## 2026-07-10 19:21 (session 9c6433)
README.md documentation was reorganized to emphasize the anki-mcp feedback loop example by moving it from inline text to a dedicated sub-paragraph within its parent section in "#what it does today"; the broken submodule link was also fixed. This change was committed and pushed to `origin/master` as `fe8aa14`. The `.claude/health-agent/*` files remain unstaged from earlier unrelated work and should be addressed separately if needed.

