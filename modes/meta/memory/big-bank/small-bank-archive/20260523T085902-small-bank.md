## 2026-05-07 15:35 (session 0531a3)
The session systematized improvements to architect and builder modes by leveraging accumulated knowledge from three now-mature subsystems (memory bank, utilities, anki-mcp). After exploring the meta system and recent structural changes, the assistant updated five files: architect context.md gained a layer model that names the new sidebar structures and a structural lessons section covering orchestrator patterns and behavior-as-data; builder context.md refactored its entry contract to point to actual files and reframed scope by function rather than path; security.md and the new mcp.md corrected stale references and added MCP protocol guidance; and product-vision.md reframed the execution pillar around pipelines and MCP integration. The meta-system documentation is now current and reflects the project's actual structure and operation.

## 2026-05-10 20:53 (session fcdea3)
User explored MCP prompts by learning about the three MCP primitives—tools (model-triggered), resources (reference data), and prompts (user-triggered conversation starters)—and compared them against skills to understand their practical differences. The key insight: prompts inject formatted text before a conversation starts, making them faster for read-aggregate-format work, while skills enable orchestration with decisions and branching; for the user's setup, prompts are mainly conversation starters without much additional value beyond that use case. The user also asked about using git submodules for the anki-connect clone within anki-mcp but didn't receive a definitive recommendation on that approach. The learning goal was satisfied—prompts make sense for their intended purpose, but skills remain the primary tool for complex operations.

## 2026-05-11 10:19 (session 16949c)
User designed and approved an architecture to automate YouTube-to-Anki phrase extraction using Gemini, with a subsystem pattern that hides the Python venv and API connectivity behind clean utility functions. The implementation is complete across `.claude/gemini/` (venv-owning subsystem with typed Part constructors for Gemini video API), `.claude/skills/youtube-extract/` (extraction skill), and `utils/llm_triggers.py` (re-export wrappers). The feature is ready to use after one-time venv setup and setting `GOOGLE_API_KEY`; the google-genai SDK API call structure was verified and corrected from dict form to typed constructors mid-implementation.

## 2026-05-11 10:42 (session c99276)
The session extended `.claude/gemini/gemini.py` with model listing and availability probing capabilities: `list_models()` returns all available models for the current API key, while `probe_model(model)` sends a minimal request to check availability and quota status with structured error reporting. A CLI tool (`gemini/info.py`) was added to support `--list` and `--probe` modes, and the functions were re-exported in `utils/llm_triggers.py` for access throughout the project. Testing revealed eight models currently available on the key (`gemini-2.5-flash`, `gemini-flash-latest`, etc.), with quota probing showing that exact remaining counts exist only in Google AI Studio but the probe endpoint reliably surfaces quota violations. The `/youtube-extract` skill invocation with a Spanish video remains pending.

## 2026-05-11 10:55 (session a8255f)
The youtube-extract skill ran into a timeout issue where the background Bash default (120s) was too short for the Gemini video pipeline (~2–3 min), causing the process to be killed before output could be written. The assistant debugged the root cause, re-ran in foreground with a longer timeout (180s), and added a note to the skill spec documenting that the extractor is a long-running foreground-only command requiring `timeout=300000`. There are 5 unreviewed health findings from recent skill runs pending review via `/health-agent`.

## 2026-05-12 21:08 (session d51a45)
Manual testing comparing the Gemini web UI with `_worker.py`'s text and video modes revealed that the video mode (using FileData with YouTube URLs) actually works correctly and extracts the right content (animal idioms with proper Spanish definitions), while text mode fails by hallucinating generic Argentine slang lists with English responses—the model cannot actually access the video when given only text. The web UI produces the highest quality output with proper Rioplatense register. The corrected understanding is that video mode via FileData is the correct approach for video extraction; text mode should not be used for videos since it has no actual access to the video content and falls back to training data hallucination.

## 2026-05-12 22:32 (session 86b40c)
The add-cards pipeline successfully generated and pushed 27 Spanish idiom cards to the Español deck. During execution, a security bug was discovered and fixed where the `generate-cards` and `anki-add-cards` skills were attempting to read from `/tmp/` (blocked by security); both skills were updated to use `.claude/tmp/` instead for temporary context files. Five pending health-agent findings from recent skill runs remain to be reviewed.

## 2026-05-13 16:59 (session 9fa9bb)
The user shifted the YouTube extraction skill design from hardcoded Spanish-specific logic toward a generalized composition system using `#include` preprocessor directives instead of folder-hierarchy-based inheritance. The core insight is that extraction and card generation need only specific deck context pieces (focus area, input/output format) rather than the full inheritable file stack. The plan evolved to build a new `preprocess.py` script to replace both `depolymorphize.py` and `find-inheritables.py`, enabling deck context files to be modular and reusable across skills. Implementation was about to begin with `preprocess.py` as the foundation, though the user noted an open question about how "user input syntax" sections scattered across `decks/` and `decks/languages/` would be handled in the new system, hinting at a need for something like virtual-function composition.

## 2026-05-21 23:12 (session 5125d0)
The user established a new plugin-based context compilation subsystem at `.claude/plugins/context-compiler/` that generalizes the prior separate compile and compile-deck-context skills into a single Python-driven preprocessing pipeline. The plugin system now auto-discovers plugins via a shell function in the profile, and the two context compilation skills are accessible as `/context-compiler:compile` and `/context-compiler:compile-deck-context`. Two key tasks remain incomplete: adding `#include` directives to the four non-root inheritable files to make the preprocessor functional, and sourcing the updated profile to activate the plugin discovery system.

## 2026-05-21 23:33 (session f372a1)
Refactored the deck configuration inheritance system: removed the `inheritable-` naming convention and replaced it with `#include` directives for explicit dependency wiring between files (e.g., `spanish.md` includes `language-defaults.md`). Moved `compiled-is-fresh.py` from scripts into the context-compiler plugin and rewrote it as a generic source-file dependency checker; correspondingly renamed `compile-deck-context` skill to `compile-context` and updated the compile pipeline to use a standardized naming chain (`<stem>.md` → `.preprocessed.md` → `.compiled.md`). Deleted the depolymorphize script and old stale output files, streamlining the build infrastructure. The skills and plugin are now fully functional, though integration testing across the full deck compilation pipeline would confirm the refactor is production-ready.

## 2026-05-21 23:41 (session fde48b)
The session addressed housekeeping around generated artifacts: youtube-phrases.txt from Gemini extraction runs was removed from git tracking but preserved on disk, with a gitignore pattern added to prevent future tracking across all language decks. The work focused on cleanup and proper artifact handling rather than new feature development. There are 5 unreviewed health findings pending that should be reviewed when convenient.

## 2026-05-21 23:57 (session ef71d8)
The user clarified the path resolution semantics for the `#include` directive in the context-compiler plugin: relative paths starting with `./` or `../` resolve relative to the including file, while paths without a leading dot (like `decks/languages/spanish/foo.md`) resolve relative to the project root. This gives full flexibility for organizing includes—both file-local and project-wide includes are supported. The clarification was prompted by the recent refactor that introduced `#include` directives to replace the old `inheritable-` naming convention.

## 2026-05-22 00:05 (session bbbd0c)
The user clarified the architecture of include directive handling in the context-compiler plugin: `#include` directives are resolved entirely at the preprocessing phase, where `preprocess.py` expands them inline and outputs a clean `.preprocessed.md` with no directives remaining. The compilation phase only reads preprocessed output, maintaining a clean separation between include resolution (preprocessor) and semantic merging (compiler). This design was confirmed to already be in place and working as intended. The user also has 5 pending health findings from skill runs to review when ready.

## 2026-05-22 00:15 (session d7c0ed)
The user completed a refactoring session focused on modularizing the deck compilation infrastructure by extracting the `#include` graph traversal logic into a dedicated `include_graph.py` module and restructuring deck configuration files to use `#include` directives. After running multiple `/compile-context` iterations to verify the Spanish deck compiled correctly, they moved into builder mode and confirmed satisfaction with the changes. The assistant provided a detailed commit message documenting the extraction and refactoring work, but as per project rules, git staging and committing remain the user's responsibility. Five pending health findings from skill runs should be reviewed when ready.

## 2026-05-23 08:41 (session 196d54)
User established a new `.claude/world_adoption/` mode to explore go-to-market and adoption strategies for the project, mirroring the structure of existing architect and builder modes but with its own isolated memory bank at `.claude/world_adoption/memory/`. The assistant verified the default trigger pattern in `registry.py` works correctly for the new mode and created the missing mode-level `context.md` entry point to instruct reading the memory bank on entry, plus updated the main `CLAUDE.md` to navigate to the mode. The infrastructure is now functional: the memory bank will trigger correctly when the mode is entered, following the same pattern as architect/builder modes. User can now scatter files in world_adoption (summaries, brainstorms, adoption ideas) while the memory bank collects and surfaces relevant institutional knowledge.

