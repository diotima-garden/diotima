# Scraped subject/people data — kept locally, never tracked (contains personal names).
*.csv
*.json
*.jsonl

# Private clone of github.com/baturevychvitalii/personal-register — versioned in its own
# repo, not here. Never tracked, so a plain clone of this project never sees it.
personal-register/
