# Utilities Awareness

Before writing any helper or shared code, check whether `.claude/utils/` already covers it.

**How:** Scan `.claude/utils/README.md` (one table). If a module looks relevant, open the source — files are short. Only write new helper code when nothing fits.
