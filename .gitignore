# Local Claude settings (machine-specific, ephemeral permissions)
.claude/settings.local.json

todo

# Anki backups (binary .apkg files, large — restore from AnkiWeb if needed)
decks/**/backups/*.apkg

# Python
__pycache__/
*.pyc

# OS
.DS_Store

# all logs
*.log

## Mem bank
**/mem-bank/small-jobs.json
**/small-bank.md

## Health agent
.claude/health-agent/run-summary.json
.claude/health-agent/permission-events.jsonl
.claude/health-agent/pending-ai-review.flag


# Anki MCP subsystem — venv (reproducible via README setup step)
.venv/

# session_crawler subsystem — venv (reproducible via README setup step)
.claude/session_crawler/.venv/

# gemini subsystem — venv (reproducible via .claude/gemini/README.md)
.claude/gemini/.venv/

# Depolymorphized context snapshots (generated, not source)
depolymorphized.md
compiled.md

